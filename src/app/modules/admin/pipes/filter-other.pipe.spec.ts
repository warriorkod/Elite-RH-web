import { FilterOtherPipe } from './filter-other.pipe';

describe('FilterOtherPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOtherPipe();
    expect(pipe).toBeTruthy();
  });
});
